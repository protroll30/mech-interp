# mech-interp

TransformerLens scratch work: induction heads on nonsense copy prompts, plus a minimal IOI-style John/Mary patching suite on GPT-2 small.

- `induction_scripts/` : scripts and `induction_circuit_writeup.md`
- `ioi_scripts/` : scripts and `ioi_circuit_writeup.md`

Install deps from `requirements.txt` (use a venv if you like).
